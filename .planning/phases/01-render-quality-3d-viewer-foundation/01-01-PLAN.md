---
phase: 01-render-quality-3d-viewer-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitattributes
  - package.json
  - tsconfig.json
  - vite.config.ts
  - index.html
  - src/main.tsx
  - src/types/index.ts
  - src/materials/metalPresets.ts
  - src/data/showcase.ts
  - src/styles/globals.css
autonomous: true
requirements:
  - INFRA-01
  - RNDR-01
  - PIPE-01
  - PIPE-02

must_haves:
  truths:
    - "Project builds and dev server starts with zero errors"
    - "All 8 metal presets exist with exact values from CLAUDE.md"
    - "TypeScript interfaces match the data model from CLAUDE.md"
    - "Binary files (.glb, .hdr, .usdz) are protected from Git line-ending corruption"
    - "Static trophy data file exports typed showcase array"
  artifacts:
    - path: ".gitattributes"
      provides: "Binary file protection for 3D assets"
      contains: "*.glb binary"
    - path: "src/types/index.ts"
      provides: "Shared TypeScript interfaces"
      exports: ["PublicTrophy", "MetalPreset", "ClientProject", "DesignOption", "Iteration"]
    - path: "src/materials/metalPresets.ts"
      provides: "MeshPhysicalMaterial preset configs for all 8 metals"
      exports: ["METAL_PRESETS"]
    - path: "src/data/showcase.ts"
      provides: "Static public trophy data"
      exports: ["SHOWCASE_TROPHIES"]
    - path: "src/styles/globals.css"
      provides: "CSS custom properties for dark luxury theme"
      contains: "--color-gold"
    - path: "package.json"
      provides: "Project dependencies"
      contains: "@react-three/fiber"
  key_links:
    - from: "src/materials/metalPresets.ts"
      to: "src/types/index.ts"
      via: "import MetalPreset type"
      pattern: "import.*MetalPreset.*from.*types"
    - from: "src/data/showcase.ts"
      to: "src/types/index.ts"
      via: "import PublicTrophy type"
      pattern: "import.*PublicTrophy.*from.*types"
---

<objective>
Scaffold the entire project from scratch and establish all foundational data contracts, type definitions, metal material presets, and static data that every subsequent plan depends on.

Purpose: This is a greenfield project with no package.json, no config files, and empty src/ directories. Everything downstream (the 3D viewer, routing, pages) needs TypeScript types, material configs, and a working build pipeline first. .gitattributes must be committed BEFORE any binary files are tracked by Git.

Output: A buildable Vite + React + TypeScript project with all dependencies installed, all shared types defined, all 8 metal presets configured, static trophy data populated, CSS theme tokens set, and .gitattributes protecting binary assets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-render-quality-3d-viewer-foundation/01-CONTEXT.md
@.planning/phases/01-render-quality-3d-viewer-foundation/01-RESEARCH.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite project, install dependencies, configure .gitattributes</name>
  <files>.gitattributes, package.json, tsconfig.json, vite.config.ts, index.html, src/main.tsx</files>
  <read_first>
    - CLAUDE.md (tech stack versions, file structure, conventions)
    - .planning/phases/01-render-quality-3d-viewer-foundation/01-RESEARCH.md (verified library versions, installation commands)
  </read_first>
  <action>
1. Create `.gitattributes` FIRST (per D-12 -- must exist before any binary file is committed):

```gitattributes
# Prevent Git from corrupting binary 3D assets
*.glb binary
*.gltf binary
*.hdr binary
*.exr binary
*.usdz binary
*.wasm binary

# Image assets
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
```

2. Scaffold with Vite 8 react-ts template:
```bash
npm create vite@latest . -- --template react-ts
```
This will create package.json, tsconfig.json, vite.config.ts, index.html, src/main.tsx, src/App.tsx.

3. Install core dependencies:
```bash
npm install three@^0.183.2 @react-three/fiber@^9.5.0 @react-three/drei@^10.7.7 @react-three/postprocessing@^3.0.4 react-router@^7.13.2
npm install -D @types/three@^0.183.1
```

4. Update `tsconfig.json` to enable strict mode (per CLAUDE.md conventions):
- Set `"strict": true`
- Ensure `"moduleResolution": "bundler"` and `"target": "ESNext"`

5. Update `vite.config.ts` to a clean config:
```typescript
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
})
```

6. Update `index.html` to set the page title to "Thomas Lyte | 3D Trophy Gallery" and ensure the root div exists.

7. Update `src/main.tsx` to a minimal React 19 entry:
```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './styles/globals.css'
import App from './App'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>
)
```

8. Create a placeholder `src/App.tsx` that renders a div with text "Thomas Lyte 3D Gallery" so the dev server has something to display.

9. Delete any Vite template boilerplate files: `src/App.css`, `src/index.css`, `src/assets/react.svg`, `public/vite.svg` if generated.
  </action>
  <verify>
    <automated>cd '/c/Users/alex.marley/The Phoenix Luxury Co. Ltd t a Thomas Lyte/Workshop - Documents/Alex Marley/AI design/3D Design' && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - .gitattributes contains "*.glb binary" and "*.hdr binary" and "*.usdz binary"
    - package.json contains "@react-three/fiber", "@react-three/drei", "@react-three/postprocessing", "three", "react-router"
    - package.json contains "@types/three" in devDependencies
    - tsconfig.json contains "strict": true
    - src/main.tsx contains "import './styles/globals.css'"
    - src/main.tsx contains "import App from './App'"
    - npm run build exits with code 0
    - index.html contains "Thomas Lyte"
  </acceptance_criteria>
  <done>Project scaffolded with all dependencies, builds without errors, .gitattributes protects binary files</done>
</task>

<task type="auto">
  <name>Task 2: Create TypeScript interfaces, metal presets, static data, and CSS theme</name>
  <files>src/types/index.ts, src/materials/metalPresets.ts, src/data/showcase.ts, src/styles/globals.css</files>
  <read_first>
    - CLAUDE.md (Data Model section for exact TypeScript interfaces, Design Theme table for CSS tokens, Rendering section for metal preset values)
    - src/main.tsx (verify import path for globals.css)
  </read_first>
  <action>
1. Create `src/types/index.ts` with ALL interfaces from CLAUDE.md Data Model section:

```typescript
export type MetalPreset =
  | 'polishedGold' | 'polishedSilver' | 'chrome'
  | 'brushedSilver' | 'brushedGold' | 'roseGold'
  | 'platinum' | 'antiqueBronze'

export interface PublicTrophy {
  slug: string
  name: string
  description: string
  modelPath: string
  usdzPath?: string
  thumbnailPath: string
  dimensions: { heightMm: number; widthMm: number; depthMm: number }
  materials: { primary: MetalPreset; secondary?: MetalPreset }
}

export interface ClientProject {
  code: string
  passcode: string
  clientName: string
  projectName: string
  designOptions: DesignOption[]
  dateCreated: string
}

export interface DesignOption {
  slug: string
  name: string
  iterations: Iteration[]
}

export interface Iteration {
  slug: string
  label: string
  modelPath: string
  usdzPath?: string
  thumbnailPath: string
  dimensions: { heightMm: number; widthMm: number; depthMm: number }
  materials: { primary: MetalPreset; secondary?: MetalPreset }
  dateAdded: string
}
```

2. Create `src/materials/metalPresets.ts` with all 8 presets (per D-09) using exact values from CLAUDE.md Rendering table:

```typescript
import type { MeshPhysicalMaterialParameters } from 'three'
import type { MetalPreset } from '../types'

export const METAL_PRESETS: Record<MetalPreset, MeshPhysicalMaterialParameters> = {
  polishedGold: {
    color: '#FFD700',
    metalness: 1.0,
    roughness: 0.05,
    clearcoat: 0.3,
    clearcoatRoughness: 0.05,
    envMapIntensity: 1.5,
  },
  polishedSilver: {
    color: '#C0C0C0',
    metalness: 1.0,
    roughness: 0.05,
    clearcoat: 0.3,
    clearcoatRoughness: 0.05,
    envMapIntensity: 1.5,
  },
  chrome: {
    color: '#FFFFFF',
    metalness: 1.0,
    roughness: 0.0,
    clearcoat: 1.0,
    clearcoatRoughness: 0.0,
    envMapIntensity: 2.0,
  },
  brushedSilver: {
    color: '#C0C0C0',
    metalness: 1.0,
    roughness: 0.35,
    clearcoat: 0.1,
    clearcoatRoughness: 0.2,
    envMapIntensity: 1.2,
    anisotropy: 0.8,
  },
  brushedGold: {
    color: '#FFD700',
    metalness: 1.0,
    roughness: 0.35,
    clearcoat: 0.1,
    clearcoatRoughness: 0.2,
    envMapIntensity: 1.2,
    anisotropy: 0.8,
  },
  roseGold: {
    color: '#B76E79',
    metalness: 1.0,
    roughness: 0.08,
    clearcoat: 0.3,
    clearcoatRoughness: 0.05,
    envMapIntensity: 1.5,
  },
  platinum: {
    color: '#E5E4E2',
    metalness: 1.0,
    roughness: 0.12,
    clearcoat: 0.2,
    clearcoatRoughness: 0.1,
    envMapIntensity: 1.3,
  },
  antiqueBronze: {
    color: '#CD7F32',
    metalness: 1.0,
    roughness: 0.4,
    clearcoat: 0.0,
    envMapIntensity: 1.0,
  },
}
```

3. Create `src/data/showcase.ts` with a test trophy entry using SheenChair.glb (per D-01):

```typescript
import type { PublicTrophy } from '../types'

export const SHOWCASE_TROPHIES: PublicTrophy[] = [
  {
    slug: 'test-trophy',
    name: 'Development Test Model',
    description: 'SheenChair.glb used for rendering validation during development.',
    modelPath: '/models/SheenChair.glb',
    thumbnailPath: '/images/placeholder.jpg',
    dimensions: { heightMm: 300, widthMm: 200, depthMm: 200 },
    materials: { primary: 'polishedGold' },
  },
]
```

4. Create `src/styles/globals.css` with CSS custom properties matching CLAUDE.md Design Theme table:

```css
:root {
  --bg-primary: #0d0d0f;
  --bg-secondary: #161619;
  --bg-tertiary: #1e1e22;
  --color-gold: #c9a962;
  --color-gold-light: #ddc07a;
  --color-gold-dark: #a8893f;
  --text-primary: #ffffff;
  --text-secondary: #a0a0a8;
  --font-heading: 'Playfair Display', serif;
  --font-body: 'Inter', sans-serif;
  --radius: 12px;
  --shadow-card: 0 4px 24px rgba(0, 0, 0, 0.4);
}

*,
*::before,
*::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html,
body,
#root {
  width: 100%;
  height: 100%;
  overflow: hidden;
  background-color: var(--bg-primary);
  color: var(--text-primary);
  font-family: var(--font-body);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
}
```
  </action>
  <verify>
    <automated>cd '/c/Users/alex.marley/The Phoenix Luxury Co. Ltd t a Thomas Lyte/Workshop - Documents/Alex Marley/AI design/3D Design' && npx tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - src/types/index.ts exports MetalPreset, PublicTrophy, ClientProject, DesignOption, Iteration
    - src/materials/metalPresets.ts contains "polishedGold" with color "#FFD700", metalness 1.0, roughness 0.05
    - src/materials/metalPresets.ts contains "chrome" with roughness 0.0, clearcoat 1.0, envMapIntensity 2.0
    - src/materials/metalPresets.ts contains "brushedSilver" with anisotropy 0.8
    - src/materials/metalPresets.ts contains "antiqueBronze" with clearcoat 0.0
    - src/materials/metalPresets.ts contains all 8 presets: polishedGold, polishedSilver, chrome, brushedSilver, brushedGold, roseGold, platinum, antiqueBronze
    - src/data/showcase.ts contains "SheenChair.glb" and imports PublicTrophy from '../types'
    - src/styles/globals.css contains "--color-gold: #c9a962" and "--bg-primary: #0d0d0f"
    - src/styles/globals.css contains "--font-heading: 'Playfair Display'"
    - npx tsc --noEmit exits with code 0
  </acceptance_criteria>
  <done>All shared types, 8 metal presets with exact CLAUDE.md values, static trophy data with test model, and CSS theme tokens are in place. TypeScript compiles cleanly.</done>
</task>

</tasks>

<verification>
1. `npm run build` exits with code 0
2. `npx tsc --noEmit` exits with code 0 (type checking passes)
3. `npm run dev` starts without errors (dev server serves the placeholder App)
4. .gitattributes exists and contains binary rules for .glb, .hdr, .usdz
5. All 8 metal presets present in metalPresets.ts with metalness: 1.0
6. Types file exports all 5 interfaces/types from CLAUDE.md data model
</verification>

<success_criteria>
- Project builds and dev server starts with zero TypeScript or build errors
- .gitattributes committed before any binary tracking
- All downstream plans can import from src/types, src/materials/metalPresets, src/data/showcase, and src/styles/globals.css without modification
</success_criteria>

<output>
After completion, create `.planning/phases/01-render-quality-3d-viewer-foundation/01-01-SUMMARY.md`
</output>
